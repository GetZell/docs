---
title: "Coaching Insights"
description: "Understand team coaching performance, signals, snippets, engagement, and coaching focus"
icon: "chart-line"
---

## Overview

Coaching Insights supports both the manager-facing dashboard for finding coaching opportunities across reps and the current-user coaching workspace for an individual rep's own data. The personal workspace is the default view at `/calls`; the historical calls table is available at `/calls?view=list`. The manager dashboard is used inside the unified team Reports workspace at `/admin/team/reports`:

- `type=simulations` mounts the tab with `source=SIMULATION`.
- `type=real-calls` mounts the tab with `source=REAL_CALL`.

Coaching Insights answers four main questions:

- Which reps have the highest coaching potential?
- Which signals are weakest across the selected team?
- Which concrete snippets should a manager review with a rep?
- What coaching opportunities can the current user review for themselves?
- Which personal focus areas and call moments should the current user review next?

## Key Concepts

<CardGroup cols={2}>
  <Card title="Zell Score" icon="zap">
    Coaching potential for a call. Higher means there is more coaching need because the call score has room to improve and the moment is coachable.
  </Card>

  <Card title="Signal Score" icon="signal">
    Average evaluation score for a signal, shown at team level and member level.
  </Card>

  <Card title="Engagement" icon="check-check">
    A 0-10 score based on how many surfaced coachable snippets were read by users.
  </Card>

  <Card title="Coachable Snippet" icon="message-square-quote">
    A low-scoring, high-coachability feedback moment with transcript context, score, coachability, replies, votes, and recording audio when available.
  </Card>
</CardGroup>

---

## Dashboard Sections

| Section | What it shows | Source data |
| --- | --- | --- |
| Stats cards | Average performance, average coaching potential, and average engagement. | Overview endpoint aggregates scored valid calls and coachable snippets. |
| Weak signals | Lowest-performing signals for the selected scope. | Evaluation categories grouped by signal. |
| Score trend | Daily average score, Zell Score, and call count. | Scored valid calls grouped by started date. |
| Team performance | Reps ordered by average Zell Score. | Per-user call evaluation aggregates. |
| Member detail | Selected rep's signal breakdown and engagement. | Per-signal scores, snippet counts, and read counts. |
| Snippets modal | Up to 50 coachable snippets for a selected rep and signal. | Snippet evaluations plus hydrated transcript, replies, votes, reviewer, and audio. |
| Personal workspace summary | Current user's total moments, reviewed moments, focus areas, and review progress. | Current-user member detail aggregated in the frontend. |
| Personal skill gaps layout | The current user's focus areas and selected signal snippets in one inline workspace. | Current-user signal breakdown plus user-scoped snippet endpoint. |

---

## Filters

Coaching Insights combines global report filters with tab-local filters.

| Filter | Where it comes from | Backend behavior |
| --- | --- | --- |
| Date range | Unified Reports global filters | Applies to `Call.startedAt`. Previous-period comparisons are only returned when both `dateFrom` and `dateTo` are present. |
| Source | Unified Reports report type | `SIMULATION` when `type=simulations`, `REAL_CALL` when `type=real-calls`. |
| Groups | Unified Reports global filters | Intersects the selected group members with any selected users. |
| Users | Coaching Insights tab | Restricts overview results to selected users. |
| Scorecards | Coaching Insights tab | Matches either the call agent's scorecard or `call.metadata.scorecardId`. |
| Workspace | Request context | If a workspace context exists, overview is scoped to that workspace's users. |

<Note>
When group and user filters are both active, the backend intersects them. If the intersection is empty, the overview returns empty stats and empty lists.
</Note>

---

## Permissions And Scope

The admin Coaching Insights router requires team-lead or admin access.

| Role behavior | Rule |
| --- | --- |
| Admin | Can query the tenant-scoped data allowed by the provided filters. |
| Team lead | Server-side scope is restricted to the team lead subtree. |
| Team lead overview | Requested users are intersected with the subtree. If no users are requested, the subtree is used. |
| Team lead member detail | Returns an empty member detail when the requested user is outside the subtree. |
| Team lead snippets | Returns `{ snippets: [] }` when the requested user is outside the subtree. |

All overview-level team aggregates only include regular users with `role = USER` and `isTestUser = false`.

---

## Current-User Endpoints

The non-admin Coaching Insights router exposes read-only endpoints for the authenticated user's own coaching data.

| Endpoint | Auth | Valid query parameters | Scope behavior |
| --- | --- | --- | --- |
| `GET /coaching-insights/overview` | `requireAuth` | `scorecardIds`, `dateFrom`, `dateTo`, `source` | Forces `userIds` to the current user. If the request has workspace context, overview data is also scoped to that workspace. |
| `GET /coaching-insights/me` | `requireAuth` | `scorecardIds`, `dateFrom`, `dateTo`, `source` | Loads member detail for the current user only. No `userId` path or query parameter is accepted. |
| `GET /coaching-insights/me/snippets?signalId={signalId}` | `requireAuth` | `signalId`, `scorecardIds`, `dateFrom`, `dateTo`, `source` | Loads snippets for the current user and uses the current user as the snippet viewer. |

Validation is handled by `UserCoachingInsightsOverviewSchema`, `UserCoachingInsightsDetailSchema`, and `UserCoachingInsightsSnippetsSchema`. `signalId` is required and must be a UUID for `/coaching-insights/me/snippets`; invalid values return a bad request before the service layer runs.

Current-user routes intentionally do not expose `groupIds`, `userIds`, `workspaceIds`, member `userId` path parameters, or coaching-focus mutation endpoints. They share the same overview, member-detail, and snippet response schemas as the admin routes.

---

## Personal Calls Workspace

The Calls page mounts `UserCallsWorkspace`. It reads the `view` search parameter and renders Coaching Insights unless `view=list`.

| UI area | Frontend component | Backend calls |
| --- | --- | --- |
| View toggle | `UserCallsWorkspace` | No backend call. `/calls` is coaching; `/calls?view=list` is the table view. |
| Workspace summary | `UserCoachingWorkspaceSummary` | Uses `GET /coaching-insights/me` through `useUserCoachingInsightsDetail`. |
| Skill gaps | `SkillGapsLayout` | Shows focus areas in a left column and the selected signal's snippets inline with `SnippetsList scope="user"`. |
| Snippets list | `SnippetsList`, `useScopedCoachingInsightsSnippets` | Uses `GET /coaching-insights/me/snippets?signalId=...` when `scope="user"`; admin views keep using the admin member snippets endpoint. |

Signals in the personal workspace are sorted by highest `snippetCount`, then by lowest `avgScore`. The personal workspace no longer stores or restores a layout choice; it only keeps the selected signal in component state. When there is no selected signal state, the first sorted signal is selected by default.

### Personal Workspace Filters

| Label | Frontend hook/component | Request field | Backend endpoint/schema/service | Database fields and scope | Defaults and empty/null behavior | Permissions and edge cases |
| --- | --- | --- | --- | --- | --- | --- |
| Scorecard | `UserCoachingInsightsDisplay` uses `MultiSelect`; `useUserCoachingInsightsViewModel` stores `calls-coaching-insights-scorecard-ids`; options come from `useScoreCardsUsedByUser`. | `scorecardIds`, comma-separated by `buildUserCoachingInsightsParams`. | `GET /coaching-insights/me` and `GET /coaching-insights/me/snippets`; `UserCoachingInsightsDetailSchema` and `UserCoachingInsightsSnippetsSchema`; `CoachingInsightsService.getMemberDetail/getCoachableSnippets`. Options load from `GET /score-cards/used-by-user`. | Filters `Agent.scorecardId` or `Call.metadata.scorecardId` in Coaching Insights SQL. Current-user endpoints force the authenticated user as the member/viewer. | Default is `[]`; empty arrays omit `scorecardIds`. If no scorecards are available, the dropdown is empty and the request is unfiltered by scorecard. | Requires `requireAuth`. The option source currently returns scorecards from ended simulation calls with agent scorecards, so real-call-only scorecards may be absent from the personal workspace filter. |
| Source | `UserCoachingInsightsDisplay` select; `useUserCoachingInsightsViewModel` stores `calls-coaching-insights-source`. | `source` with `REAL_CALL` or `SIMULATION`; omitted for `all`. | `GET /coaching-insights/me` and `/me/snippets`; current-user schemas use the `CallSource` enum. | Filters `Call.source` in member detail and snippet SQL. | Default is `all`, which sends `undefined` and includes both sources. | Invalid enum values fail schema validation. The Calls History table does not currently expose this source filter. |
| Date range | `DateRangeDropdown`; `useDateRangeWithStorage` stores `calls-coaching-insights-date-range`; `formatDateForApi` serializes dates. | `dateFrom`, `dateTo` as ISO datetime strings without milliseconds. | `GET /coaching-insights/me` and `/me/snippets`; current-user detail/snippets schemas; `CoachingInsightsService` date filters. | Filters `Call.startedAt`. Previous-period values are only meaningful when both dates are present. | Default is last 7 days. Missing `from` or `to` omits that side. Invalid local-storage JSON is removed and the default remains. | Future dates are disabled; custom future end dates are clamped to today. |
The personal snippets list sorts loaded snippets so snippets with replies appear first. Score badges are hidden in user scope (`showScoreBadges={false}`), while admin scope keeps score badges visible.

---

## Score Calculations

### Zell Score

Zell Score is computed from call score and coachability:

```text
zellScore = (10 - callScore) * (coachability / 10)
```

The stored value is rounded to two decimals. A high Zell Score means the call has both low performance and high coachability.

### Coachability

Call coachability is computed from snippet-level coachability, weighted by the score gap:

```text
gap = 10 - snippetScore
coachability = weighted average of snippetCoachability by gap
```

Snippets with a perfect score do not contribute because their score gap is `0`.

### Average Performance

Average performance uses `CallEvaluation.totalScore` from valid calls with non-null total score. The overview stats card computes the displayed team average from the daily trend values and rounds it to one decimal.

### Average Coaching Potential

Average coaching potential is the average of member-level `avgZellScore` values in the current result set. Each member's value is rounded to two decimals.

---

## Engagement

Engagement measures whether users read the coachable snippets Zell surfaced.

| Rule | Behavior |
| --- | --- |
| Eligible snippet | `SnippetEvaluation.score <= 5` and `coachability > 5`. |
| Excluded snippet | Any snippet with a `DISLIKE` vote is excluded. |
| Read snippet | Eligible snippet whose feedback has `readAt` set. |
| Daily cap | At most `10` surfaced snippets per user per day count toward engagement. |
| Score scale | Engagement is displayed on a 0-10 scale. |

The daily cap is computed as:

```text
10 * numberOfDaysInDateRange
```

If no explicit date range is provided, the cap is `10`.

Team engagement sums capped snippet counts per user, then computes:

```text
engagement = round((totalRead / totalSnippets) * 100) / 10
```

That means `10.0` is fully read, `5.0` is half read, and `0.0` means no eligible snippets were read.

---

## Weak Signals

Weak signals show the lowest-scoring signals across the selected scope.

| Rule | Behavior |
| --- | --- |
| Source | `EvaluationCategory` rows joined to valid scored calls. |
| Required data | `score IS NOT NULL` and `signalId IS NOT NULL`. |
| Minimum sample | The signal must have at least `2` evaluations. |
| Sort order | Lowest average score first. |
| Evaluated count | Count of distinct users evaluated for that signal. |
| Previous score | Returned only when both `dateFrom` and `dateTo` are present. |

The UI displays weak signals as bars on a 0-10 scale. Green starts at `7`, amber at `4`, and red below `4`.

---

## Member Detail

Selecting a member loads that user's detailed signal breakdown.

| Field | Behavior |
| --- | --- |
| Call count | Count of valid calls with a non-null total score for that user. |
| Signal average | Average `EvaluationCategory.score`, rounded to one decimal. |
| Snippet count | Count of eligible coachable snippets for that signal. |
| Read count | Count of eligible coachable snippets with `readAt` set. |
| Signal order | Lowest average score first. |
| Previous signal score | Same signal over the previous period when a full date range exists. |

Member engagement uses the same eligible-snippet and daily-cap rules as team engagement, but only for the selected user.

---

## Coachable Snippets

The snippets modal opens from a member signal card or a personal workspace focus area and loads snippets for one user and one signal.

| Rule | Behavior |
| --- | --- |
| Required user | Admin scope uses the selected member's user ID. User scope uses the authenticated user from the current-user endpoint; the frontend still passes `userId` for shared component props. |
| Required signal | The selected signal ID. |
| Score rule | `SnippetEvaluation.score <= 5`. |
| Coachability rule | `SnippetEvaluation.coachability > 5`. |
| Vote exclusion | Snippets with any `DISLIKE` vote are excluded. |
| Sort order | Highest coachability first, then lowest score. |
| Limit | `50` snippets. |
| Hydration | Adds transcript context, replies, viewer vote, reviewer, recording URL, and call metadata. |

Snippets endpoints return an object wrapper, not a bare array:

```json
{
  "snippets": []
}
```

This applies to both `GET /admin/coaching-insights/member/{userId}/snippets` and `GET /coaching-insights/me/snippets`. For admin requests by a team lead, an out-of-subtree member request also returns `{ snippets: [] }` instead of leaking whether the user has coachable moments. For personal workspace requests, the frontend enables the user endpoint only when a `signalId` is present.

The frontend sorts loaded snippets so snippets with replies appear first. Audio playback is coordinated so only one snippet plays at a time.

<Note>
For the shared snippet hydration behavior, see [Snippet Selection Logic](/internal/features/backend-snippet-selection).
</Note>

---

## Coaching Focus

Managers can set one or more focus signals for a rep from the member detail panel.

| Action | Behavior |
| --- | --- |
| Set focus | Stores unique signal IDs on `User.coachingFocus` with `assignedBy` and `assignedAt`. |
| Clear focus | Sets `User.coachingFocus` to database null. |
| Validation | The backend checks that the user and all selected signals belong to the same tenant. |
| UI state | Focused signals show a focus badge in the member detail panel. |
| Downstream use | Feedback Inbox prioritizes snippets that match the rep's coaching focus. |

The backend supports both the current multi-signal format and an older single-signal stored format when reading focus data.

---

## Previous Period Comparisons

When both `dateFrom` and `dateTo` exist, Coaching Insights compares the selected period to the immediately preceding period with the same duration.

| Current selected period | Previous period |
| --- | --- |
| July 8 to July 15 | July 1 to July 8 |
| Last 14 days | The 14 days before that window |

Previous-period values are used for deltas on:

- average performance,
- average coaching potential,
- average engagement,
- weak signal average score,
- member signal average score,
- member engagement.

---

## Empty States

| Situation | Result |
| --- | --- |
| No users after filter intersection | Empty overview. |
| No scored valid calls | Stats are `0`, member and trend lists can be empty. |
| Personal workspace has no detail response | The frontend renders nothing after loading. |
| Personal workspace has no signals | `SkillGapsLayout` shows "No coachable moments". |
| Team lead requests member detail outside subtree | Empty member detail. |
| Team lead requests snippets outside subtree | `{ snippets: [] }`. |
| Signal has no eligible snippets | Member signal card shows no coachable moments. |
| Snippets endpoint finds none | Response is `{ snippets: [] }`; the snippets modal shows "No coachable snippets found". |

---

## Operational Notes

| Topic | Detail |
| --- | --- |
| Admin overview endpoint | `GET /admin/coaching-insights/overview` |
| Admin member detail endpoint | `GET /admin/coaching-insights/member/{userId}` |
| Admin snippets endpoint | `GET /admin/coaching-insights/member/{userId}/snippets?signalId={signalId}` |
| Focus endpoint | `GET`, `PUT`, `DELETE /admin/coaching-insights/member/{userId}/focus` |
| Current-user overview endpoint | `GET /coaching-insights/overview` |
| Current-user detail endpoint | `GET /coaching-insights/me` |
| Current-user snippets endpoint | `GET /coaching-insights/me/snippets?signalId={signalId}` |
| Overview query key | `coaching-insights-overview` plus filter values |
| Member query key | `coaching-insights-member` plus user and filter values |
| Snippet query key | `coaching-insights-snippets` plus user, signal, and filter values |
| Personal detail query key | `user-coaching-insights-detail` plus date, scorecard, and source filter values |
| Personal snippets query key | `user-coaching-insights-snippets` plus signal, date, scorecard, and source filter values |
| Focus query key | `coaching-insights-focus`, user ID |
| Personal scorecard storage | `calls-coaching-insights-scorecard-ids` |
| Personal source storage | `calls-coaching-insights-source` |
| Personal date storage | `calls-coaching-insights-date-range` |
| Default trend window | Last 8 weeks when no `dateFrom` is provided. |
| Trend weekday filter | Score trend excludes Saturday and Sunday. |

## Source References

| Area | Source |
| --- | --- |
| Coaching Insights service | `zell-backend-ts/src/services/coachingInsights/index.ts` |
| Coaching Insights SQL queries | `zell-backend-ts/prisma/queries/coachingInsightsQueries.ts` |
| Admin router and permissions | `zell-backend-ts/src/api/admin/coachingInsights/coachingInsightsRouter.ts` |
| Admin snippets subtree guard | `zell-backend-ts/src/api/admin/coachingInsights/memberSnippets/index.ts` |
| Current-user router and auth | `zell-backend-ts/src/api/coachingInsights/coachingInsightsRouter.ts` |
| Current-user overview handler | `zell-backend-ts/src/api/coachingInsights/overview.ts` |
| Current-user detail handler | `zell-backend-ts/src/api/coachingInsights/detail.ts` |
| Current-user snippets handler | `zell-backend-ts/src/api/coachingInsights/snippets.ts` |
| Router mounting | `zell-backend-ts/src/server.ts` |
| Team-lead subtree scope | `zell-backend-ts/src/api/admin/coachingInsights/filters.ts` |
| Request schemas | `zell-backend-ts/src/schemas/coachingInsightsSchemas.ts` |
| Score and coachability formulas | `zell-backend-ts/src/services/callScoring/index.ts` |
| Coaching Insights tab | `zell-frontend/src/components/teamManagement/coachingInsights/CoachingInsightsTab.tsx` |
| Stats cards | `zell-frontend/src/components/teamManagement/coachingInsights/CoachingInsightsStats.tsx` |
| Member detail panel | `zell-frontend/src/components/teamManagement/coachingInsights/MemberDetailPanel.tsx` |
| Snippets list | `zell-frontend/src/components/teamManagement/coachingInsights/SnippetsList.tsx` |
| Scoped snippets hook | `zell-frontend/src/components/teamManagement/coachingInsights/useScopedCoachingInsightsSnippets.ts` |
| Calls workspace route | `zell-frontend/src/app/(auth)/calls/page.tsx` |
| Calls workspace toggle | `zell-frontend/src/components/dashboard/user/UserCallsWorkspace.tsx` |
| Personal Coaching Insights view | `zell-frontend/src/components/dashboard/user/UserCoachingInsightsDisplay.tsx` |
| Personal Coaching Insights view model | `zell-frontend/src/components/dashboard/user/useUserCoachingInsightsViewModel.ts` |
| Personal workspace summary | `zell-frontend/src/components/dashboard/user/UserCoachingWorkspaceSummary.tsx` |
| Personal skill gaps layout | `zell-frontend/src/components/dashboard/user/coachingInsightsLearningLayouts.tsx` |
| Personal detail hook | `zell-frontend/src/hooks/api-hooks/coaching-insights/queries/useUserCoachingInsightsDetail.ts` |
| Personal snippets hook | `zell-frontend/src/hooks/api-hooks/coaching-insights/queries/useUserCoachingInsightsSnippets.ts` |
| Frontend API hooks | `zell-frontend/src/hooks/api-hooks/coaching-insights` |
